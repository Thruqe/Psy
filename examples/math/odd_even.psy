START
    num = 7
    IF num % 2 == 0 THEN
        OUTPUT "Even"
    ELSE
        OUTPUT "Odd"
    ENDIF

    num = 4
    IF num % 2 == 0 THEN
        OUTPUT "Even"
    ELSE
        OUTPUT "Odd"
    ENDIF
END